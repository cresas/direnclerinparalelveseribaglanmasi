// Direnç kombinasyonları, Türkçe metinler
// Son değişiklik 18.02.2020

// HTML yazımında metinler:

var text01 = "SIFIRLAMA"; // SIFIRLAMA
var text02 = "PİLİN GERİLİMİ:"; // Pilin gerilimi
var text03 = "DİRENÇ:"; // Direnç
var text04 = "DİRENÇ EKLE (SERİ BAĞLA)"; // Direnç ekle (seri bağla)
var text05 = "DİRENÇ EKLE (PARALEL BAĞLA)"; // Direnç ekle (paralel bağla)
var text06 = "ÖLÇÜMLER:"; // Metreler
var text07 = "GERİLİM"; // Gerilim
var text08 = "AKIM"; // Akım

var author = "";
var translator = "";

// Unicode yazımında metinler:

var text09 = "GERİLİM:"; // Gerilim
var text10 = "AKIM:"; // Akım
var text11 = "DİRENÇ:"; // Direnç
var text12 = "EŞDEĞER DİRENÇ:"; // Eşdeğer direnç
var text13 = "ÇOK KÜÇÜK"; // çok küçük
var text14 = "ÇOK BÜYÜK"; // çok büyük

var volt = "V"; // Volt
var ampere = "A"; // Ampere
var ohm = "\u03A9"; // Ohm (Unicode zaten mevcut)

// Semboller:

var decimalSeparator = ".";  // Ondalık ayırıcı (virgül/nokta)
var symbolMult = "\u00D7";  // Çarpan sembolü
